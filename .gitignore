# Config local de preview y de herramientas — NUNCA entra al repo.
# GitHub Pages sirve index.html + js/*.js desde la raíz; no depende de nada de .claude/.
.claude/launch.json
.claude/settings.local.json

# Node (solo para `npm test`, no producción)
node_modules/
